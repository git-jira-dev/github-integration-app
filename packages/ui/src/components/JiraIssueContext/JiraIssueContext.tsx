import { Stack } from '@atlaskit/primitives';
import PullRequests from '@/components/JiraIssueContext/components/PullRequests';

const JiraIssueContext = () => {
  return (
    <Stack>
      <PullRequests />
    </Stack>
  );
};

export default JiraIssueContext;
